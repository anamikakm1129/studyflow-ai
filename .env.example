# Optional overrides for docker-compose.yml. Copy this to ".env" in this
# same folder if you want to change the default ports; otherwise the
# defaults below are used automatically.

BACKEND_PORT=8000
FRONTEND_PORT=8080
