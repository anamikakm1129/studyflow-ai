# Base URL of the FastAPI backend.
# Leave unset in local dev to use Vite's proxy (see vite.config.js).
# In production, point this to your deployed backend (e.g. AWS App Runner URL).
VITE_API_BASE_URL=
